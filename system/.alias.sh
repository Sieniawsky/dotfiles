# Movement
alias home='cd ~'
alias web='cd ~/web'
alias proj='cd ~/projects'

# Lit
alias l="ls -l"
alias la="ls -la"

# git
alias safe='gs && gd'
alias gsquash='git reset $(git merge-base master $(git rev-parse --abbrev-ref HEAD))'

# Dev
alias sub='sudo subl .'
